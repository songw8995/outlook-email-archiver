# 测试与限制

1.2.5 在开发机器上完成 26 项回归测试与便携启动自检，包括虚构邮件转换、重复运行、部分失败恢复、Windows 文件锁、GUI 完成提示、增量索引和中断恢复。

十万条虚构历史 CSV 索引新增一封后，索引更新约 4.46 秒，只读取一份邮件元数据。这是合成测试，不是其他磁盘或真实用户归档的性能承诺。

PST 挂载清理的回归测试使用模拟 COM。此前独立 MSG 集成测试使用由 Outlook 创建的虚构邮件，不能代替任意真实 PST 的完整验收。未在多台电脑或所有 Outlook 版本验证。

沙箱环境可能不能访问 Python 创建的私有临时目录；开发验证采用工作区内普通权限的短路径虚构临时目录。生产程序没有为此修改系统权限。

注意：`.msd` 兼容仅限内容实际为 MSG、但扩展名误写为 MSD 的文件，不是通用 MSD 格式转换器。Outlook 缓存可见邮件不代表服务器上的全部邮件。
