# 一页中文使用说明

## 启动

便携版请完整解压后双击 `Start.bat`，可自行创建桌面快捷方式。正常时打开中文窗口；若启动失败，会显示中文原因并在本机 LocalAppData 下的 OutlookEmailArchiver 文件夹留下启动日志。从源码运行请按 README 创建独立虚拟环境；源码包不包含 Python 运行环境。

## 导出当前 Outlook 邮件

1. 选“当前经典 Outlook”，点“读取邮箱和文件夹”。读取的是当前 Outlook 实际可见内容，不代表服务器全部邮件。
2. 在下拉框选择邮箱；在文件夹列表用 `Ctrl`/`Shift` 多选，也可全选当前邮箱。按需要勾选“包含子文件夹”。
3. 选择全部年份、输入多个年份（如 `2024, 2025`），或输入开始/结束日期；窗口中的结束日期包含当天。
4. 选择保存位置、目录顺序和“导出内容”。“仅 Markdown”最简洁；“完整归档”保存 MSG、附件和正文图片；三个复选框也可单独选择。第一次建立归档后，此保存位置的目录顺序和导出内容固定，想更换请选新的空目录。
5. 首次保留“首次样本最多 5 封”勾选，先点“预览”核对数量，再点“开始”。样本检查通过后取消该勾选，即可处理窗口中选定的完整范围；正式模式没有 5 封上限。小屏幕看不到下方时，用窗口右侧滚动条或鼠标滚轮。

## 导出 PST

1. 选“PST 文件”，点“选择 PST 文件”，再点“读取邮箱和文件夹”。
2. 窗口会显示“PST 工作副本”位置。默认跟随保存位置所在磁盘；例如保存位置是 `D:\Archive\MailArchive`，副本默认是 `D:\Archive\PSTWork`。也可点“选择副本位置”，点“跟随保存磁盘”可恢复自动设置。
3. 程序不会直接在源 PST 上工作：首次先检查工作副本所在磁盘的空间，制作与源 PST 同样大小的完整副本并核对 SHA-256；以后复用已验证副本。这里的空间不是 Markdown 大小。
4. 如果源 PST 正在 Outlook 中打开且还没有可复用副本，程序会要求先在经典 Outlook 中关闭该数据文件。不要复制正在使用且未验证的 PST。
5. 后续选择文件夹、时间和保存位置的方法与当前 Outlook 相同。移动过源 PST 后，请重新点“选择 PST 文件”选择新路径。

## 导出一批 MSG 文件

1. 选“MSG / MSD 文件或文件夹”。可点“选择 MSG 文件”多选，也可点“选择 MSG 文件夹”递归处理其中全部 `.msg`/`.msd`。
2. MSG 来源不需要选择邮箱和 Outlook 文件夹，直接选择时间、保存位置，再点“预览”和“开始”。
3. MSG 源文件保持不变。“完整归档”会原样复制为每封目录中的 `original.msg`，并提取正文、附件和 CID 图片；“仅 Markdown”只生成 `.md`。
4. `.msd` 只表示兼容尝试：若文件实际是改错扩展名的 Outlook MSG，可以转换；若属于其他软件的 MSD 格式，会列入失败清单，需要提供样本后再增加对应解析器。

## 停止、继续和查看结果

索引更新默认选择“增量更新”，沿用历史总目录，只读取新增或变化邮件的元数据。首次没有索引时自动完整建立；手动删除归档文件或索引异常时可选择“完整重建（较慢）”。报告写好即可查看；索引尚在更新时请勿强制关闭程序。

1.2.2 起，转换结束会弹出前置结果窗口并发出提示音（取决于系统声音设置）；关闭弹窗后，主窗口顶部仍保留结果。生成索引和报告期间会显示“正在生成索引和结果报告”。停止、失败或没有收到完成结果时会明确提示，不会显示为全部成功。

- 点“停止”后，程序会在当前单封处理完时保存检查点并停止；不要强制结束 Outlook。
- 重新打开窗口会恢复上次选择。点“继续”，完整且未变化的邮件会核验后跳过，失败/部分完成项会重试。
- 每次完成后会弹出结果窗口。可直接点“打开结果报告”或“打开失败清单”；失败清单写明主题、来源、文件夹、失败阶段、具体原因和是否可继续重试。主窗口也保留这两个按钮。
- 单封目录中：`email.md` 用于阅读，`original.msg` 用经典 Outlook 回看，`attachments/` 是普通附件，`images/` 是正文图片。不要执行不可信附件。
- 邮件名为 `年月日_主题`。只有同一位置确实同名时，后一个才增加 ` (2)`、` (3)`；这是同名处理，不是删除重复邮件。

## 重要说明

程序不会发送、删除、移动、标记已读或修改邮件，不会关闭 Outlook，不会下载外链图片，也不会上传邮件。源邮件后来删除时，本地归档不会自动删除。遇到权限、未下载、损坏、引用附件或图片无法恢复时，以报告中的“警告/失败”为准，不能把它们当作完整成功。
